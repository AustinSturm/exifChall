version: '3.7'

services:
  web:
    build: .
    command: gunicorn exif.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - ./exif/:/usr/src/exif/
    expose:
      - 8000
    environment:
      - SECRET_KEY=please_change_me

  nginx:
    build: ./nginx
    ports:
      - 80:80
    depends_on:
      - web
